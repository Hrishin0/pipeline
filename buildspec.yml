version: 0.2

env:
    LOGIN: "7cf2078ee5cbdcefea96c71d3379dac3f935024e"
    HOST: "https://sonarcloud.io"
    Organization: "Hrishin0"
    Project: "Devops-Project"

phases:
  install:
    runtime-versions:
      java: corretto17        
      python: 3.11
    commands:
      - pip install --upgrade pip
      # Install dependencies needed for running the static analysis
      # - yum update
      - yum install -y jq
      - wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.7.0.2747-linux.zip
      - unzip ./sonar-scanner-cli-4.7.0.2747-linux.zip
      - export PATH=$PATH:$PWD/sonar-scanner-4.7.0.2747-linux/bin/
  pre_build:
    commands:
      - sonar-scanner -Dsonar.organization=Hrishin0 -Dsonar.projectKey=Devops-Project -Dsonar.sources=. -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=7cf2078ee5cbdcefea96c71d3379dac3f935024e
      - sleep 5
      - curl https://sonarcloud.io/api/qualitygates/project_status?projectKey=Devops-Project > analysis.json
      - cat analysis.json
  build:
    commands:
#      - if [[ $(jq -r '.projectStatus.status' analysis.json) = "ERROR" ]] ; then CODEBUILD_BUILD_SUCCEEDING=0; fi
artifacts:
  files:
    - '**/*'
